<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MongoCrudApp</title>
    <link rel="stylesheet" href="/style/index.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900">
       <nav class="bg-gray-800 p-4">
        <div class="container mx-auto flex justify-between items-center">
            <a href="/" class="text-2xl font-bold text-blue-400">MongoCrudApp</a>
            <div>
                <a href="/" class="text-gray-300 hover:text-white mx-2">Home</a>
                <a href="/about" class="text-gray-300 hover:text-white mx-2">About</a>
                <a href="/update" class="text-gray-300 hover:text-white mx-2">Update</a>
            </div>
        </div>
    </nav>

    <div class="h-full w-full text-white p-10">
        <div class="nav mb-10">
            <a class="text-blue-400" href="/">Go back to Home</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-4">All Users</h1>
        <div class="users flex gap-3 flex-wrap">
            <% users.forEach(function(user){ %>
                <div class="user max-w-72 p-4 bg-zinc-800 rounded-lg">
                    <div class="rounded-lg w-full h-40 bg-zinc-500 overflow-hidden">
                        <img class="w-full h-full object-cover" src="<%= user.image %>" alt="">
                    </div>
                    <h3 class="text-xl tracking-tighter mt-3"><%= user.name %></h3>
                    <h5 class="text-zinc-400"><%= user.email %></h5>
                    <div class="flex gap-3 mt-3">
                        <a class="text-zinc-400" href="/update/<%= user._id %>">Edit User</a>
                        <a class="text-red-700" href="/delete/<%= user._id %>">Delete User</a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <footer class="bg-gray-800 p-4 mt-10">
        <div class="container mx-auto text-center">
            <p class="text-gray-400">&copy; 2024 MongoCrudApp. All rights reserved.</p>
        </div>
    </footer>

</body>
</html>
